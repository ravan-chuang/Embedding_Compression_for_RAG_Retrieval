-r requirements-dev.txt
# CI runs on a disposable Linux runner. Keep exactly one Faiss provider there:
# pip's Linux wheel avoids the missing-MKL shared library observed with the
# conda-forge Faiss package in GitHub Actions.
faiss-cpu>=1.9,<2.0
httpx2
